#include<stdio.h>
void main()
{
int a,b,c,sum;
printf("Enter three angles of a triangle");
scanf("%d",&a);
scanf("%d",&b);
scanf("%d",&c);
sum=a+b+c;
if(sum==180)
{printf("Triangle can be form");}
else
{printf("Triangle cannot be form");}
}
